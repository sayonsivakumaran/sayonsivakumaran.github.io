import React from 'react';
import { Layout, VerticalNavbar } from '../../components';

export default () => (
  <div className='about-page'>
    <VerticalNavbar/>
    <Layout>

    </Layout>
  </div>
);